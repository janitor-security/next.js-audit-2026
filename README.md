# vercel/next.js PR Audit Report — Janitor v8.0.13

**Date**: 2026-03-29
**PRs Audited**: 968
**Engine**: The Janitor v8.0.13 (tree-sitter AST, MinHash LSH, ML-DSA-65 attestation)

## The Bottleneck Problem

**968 pull requests** entered the `vercel/next.js` merge queue during this audit window.

At standard engineering-team review capacity — 8 PRs per engineer per day — a 4–6× AI-assisted productivity surge means the inbound queue grows faster than humans can process it. A team of 10 engineers has a review capacity of 80 PRs/day. At 5× AI throughput that queue receives 400 PRs/day and accumulates a 320-PR backlog every 24 hours. It never clears. It compounds.

Human review at AI velocity is a mathematical impossibility. The Janitor is the structural circuit breaker that moves enforcement to the diff level — before the merge button is available, at machine velocity.

## Circuit Breaker Impact

- **7.4%** of PRs intercepted upstream of human review (72 of 968)
- **2.2 hours** of senior-engineer triage time redirected to productive work
- **$1960** Total Economic Impact across audit window

## Threat Intelligence Summary

- **0** Swarm clone pairs detected (Jaccard ≥ 0.70 structural similarity)
- **398** language antipatterns found across all PRs
- **105** zombie dependencies re-introduced (declared in manifest, never imported)
- **4** Critical Threats (security antipattern or Swarm collision) — $150/intercept
- **11** Necrotic GC intercepts (bot-closeable dead-code) — $20/intercept
- **57** Structural Slop intercepts (slop_score > 0, no threat signal) — $20/intercept

## Top 10 PRs by Slop Score

*See `gauntlet_intelligence_report.pdf` for the full ranked table with antipattern breakdowns.*

Highest slop score in this audit: **990** (100-point gate = fail threshold).

## Provenance Ledger

The Gatekeeper has verified that **0 source bytes left the runner**.

Only the structural score — a signed integer and its antipattern labels — crosses the network boundary to the Governor control plane. Source code, AST nodes, and symbol names from your repository are never transmitted.

| Metric | Value |
|--------|-------|
| Source Analyzed | 705.69 MB |
| Egress to Control Plane | 0 KB |
| Exfiltration Ratio | 0.0% |
| Bytes Received | 0 bytes |

> **House Gatekeeper Attestation**: The Gatekeeper has verified that 0 source bytes left the runner. The analysis surface was 739976309 bytes of diff content. The control plane received 0 bytes of signed structural metadata. Your source code remained on your infrastructure for the entire audit.

## Methodology

The Janitor v8.0.13 runs a 6-stage structural analysis pipeline on each pull request diff:

1. **Vibe-Check Gate** — zstd compression ratio < 0.15 flags vibe-coded PRs that lack human-authored structural variance (`antipattern:ncd_anomaly`, +10 pts). Fires before tree-sitter parses a single node.
2. **AST Antipattern Scan** — tree-sitter queries for 20+ security patterns across 12 languages (memory-unsafe C/C++, subprocess injection, innerHTML XSS, open CIDR, S3 public ACLs)
3. **MinHash LSH Clone Detection** — 64-hash Jaccard index detects coordinated Swarm injection across PRs; Jaccard ≥ 0.85 = structural clone
4. **Zombie Dependency Detection** — manifest vs. import-graph cross-reference (`architecture:zombie_dependency`)
5. **Social Forensics** — unlinked PRs (+20 pts), AhoCorasick comment-violation scan
6. **Necrotic GC Gate** — semantic null analysis (base vs. head AST diff; `backlog:SEMANTIC_NULL` etc.)
7. **Data Sanitization** — The engine fetched the 1000 most recent PR references. 32 malformed or empty merge commits were filtered from the dataset prior to analysis, resulting in a cryptographically precise audit of 968 pull requests.

All analysis runs on your hardware. Source code never leaves your environment.
Zero-upload guarantee: The Janitor engine runs inside your CI runner in both CLI and Sentinel modes.

See <https://thejanitor.app/architecture> for the full technical specification.

## Artefacts in This Package

| File | Description |
|------|-------------|
| `gauntlet_intelligence_report.pdf` | Full PDF intelligence report with ranked PR table |
| `gauntlet_export.csv` | 16-column CSV audit trail (Excel/pandas compatible, UTF-8 BOM) |
| `gauntlet_report.json` | Machine-readable aggregate statistics |
| `next.js_cbom.json` | CycloneDX v1.5 Cryptography Bill of Materials |
| `next.js_intel.json` | Per-repo JSON with clone pairs and top-50 PR detail |
| `next.js_vex.json` | CycloneDX v1.5 VEX — exploitability status for all scanned threat classes |
| `case-study.md` | This document |

---

*Generated by The Janitor v8.0.13 — <https://thejanitor.app>*
